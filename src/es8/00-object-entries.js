// Objetos.
const countries = {
    MX: 'México',
    CO: 'Colombia',
    CL: 'Chile',
    PE: 'Peru'
};

console.log(Object.entries(countries));

const listCountries = Object.entries(countries);
//Obnetemos un arreglo de las llaves y los valores
console.log(listCountries[0]);